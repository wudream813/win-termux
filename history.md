# win-termux 版本历史 (Version History)

本文档记录 `win-termux` 各版本的详细更新日志与演进历程。

---

## 版本更新记录

### v1.8.31
- **① 修复宽字符换行后块选高亮整体错位一列**：当一行因为汉字（占两列）在只剩最后一列时放不下而**强制换行**，旧行最后一列没有被擦写，ConPTY 重绘时那一格会残留脏内容（可能是旧字符、宽字次格 `0` 占位，甚至半个汉字）。吸附函数 `snap_left_to_char` 只看「本格 `ch==0` 且左邻是宽字主格」就把它当成汉字次格而左退一列——于是块选经过这条「因汉字换行」的行后，**后面所有即使全是窄字符的行，高亮/复制也整体错位一列**。修复：`vt.c` 在宽字符强制换行时**先把旧行末列显式清成空格**，它就只是普通空白，吸附与渲染都不再误判。（新增 `verify_wide_wrap.py`，链接真实 vt/screen 复现并断言旧行末格被清空；变异测试验证。）
- **② 修复设置页「文字行 hover 带到右边按钮上」**：键位页整行 hover 热区（`< main_left+70`）一直盖到了右侧的 `[前缀]/[改]/[复位]` 按钮，行为页 scrollback 行（`< main_left+60`）盖到了 `[-]/[+]`。鼠标停在按钮上时，**整行底色和按钮底色同时高亮**。修复：整行 hover 只覆盖文字区（到按钮列前一列），按钮改成按行/列独立判断高亮，互不叠加。
- **③ 搜索框大小写标记可 hover / 点击**：右上角搜索框里的 **`Aa`（区分大小写，蓝）/ `aa`（忽略，灰）** 标记现在可以鼠标悬停（加深底色提示可点）和**左键点击切换**，与键盘 `Alt+C` 等价、切换后实时重算高亮。渲染、hover、点击三处共用同一命中几何（`search_box_case_hit`），高亮哪里就能点哪里。
- **验证**：新增 `verify_wide_wrap.py`；`verify_search_box.py` 加 Aa/aa 命中几何断言；`verify_settings_ui.py` 加键位页/行为页 hover 不覆盖按钮列、搜索大小写标记可点击的不变量。单元测试 260 / 0 failed，`verify_all.py` **31 项**全过，MinGW gcc / g++ 双 `-Werror`。

### v1.8.30
- **① 修正 Alt 框选「强制偶数宽」**：v1.8.28 起块选会把奇数宽度补成偶数，结果在窄字符上**按一下 → 高亮却多走一格**（与光标不同步）。宽字符一次跨两列本就由 `copy_step_char` + 渲染/复制的整字吸附保证，不需要再人为补偶数。现移除渲染高亮与复制两条路径上的「补偶数宽」逻辑，**→ 按一次高亮/复制只跟着多一格**（宽字符一次跨两列）。
- **② 修复设置页语义颜色网格的 hover 错位**：外观页「语义颜色」16 色网格的悬停热区上界比点击命中少一列——鼠标停在某个色块右半边时**不会高亮、但点击仍生效**（hover 与点击不一致）。已把 hover 列区间对齐到点击命中（`g_mouse_x ∈ [col-1, col+COL_W-3]`，与 `c ∈ [col, col+COL_W-2]` 一致）。
- **验证**：`verify_copy_mode.py` 改为断言块选**不**再强制偶数宽、且按半开 `[lo, hi-1]` 渲染（→ 一格）；`verify_settings_ui.py` 增加语义颜色网格 hover 列区间对齐点击命中的不变量。单元测试 260 / 0 failed，`verify_all.py` 30 项，gcc / g++ 双 `-Werror`。

### v1.8.29
- **① 搜索框内可手动切换「区分大小写」**：在搜索输入框按 **Alt+C**（或 Alt+L）即时切换大小写敏感，并立即按新模式实时重算高亮；搜索框后缀显示当前状态——蓝色高亮 **Aa**=区分大小写、灰色 **aa**=忽略大小写（不改变固定框宽 42 列）。
- **② 删除已无用的 copy_on_select 设置**：v1.8.28 已移除「鼠标拖动自动复制」，该设置不再有任何效果，现从设置页行为开关、`termux.ini` 读写、默认值中一并删除（行为开关重排为 mouse / copy_move_deselect / confirm_on_exit / search_case_sensitive 四项）。
- **③ Alt 框选宽度恒为 2 的倍数——即使一个汉字/标点都没框到**：此前偶数对齐只在某些情况下生效，且向【左】扩展时错误地移动了锚点（反而缩窄选区）。现在改为：奇数格数时把**活动端点**向远离锚点的方向再外扩一列（端点在右则 +1、端点在左则 −1，锚点不动），渲染高亮与复制内容都按此偶数宽度处理；纯 ASCII/标点内容同样保持偶数宽度，框边永远落在整列边界。
- **验证**：`verify_search.py` 增加 Alt+C 切换大小写、搜索框 Aa/aa 标记、copy_on_select 无残留等不变量；`verify_copy_mode.py` 更新块选偶数宽度（`endp += 1`/`endp -= 1` 按方向）断言；`verify_settings_ui.py` 更新行为页四开关并断言 copy_on_select 已删。单元测试 260 / 0 failed，`verify_all.py` 30 项，gcc / g++ 双 `-Werror`。

### v1.8.28
复制模式与搜索的五项调整：
- **① 修复搜索 U/D 方向反了**：此前「下一个」实际往上行跳、「上一个」往下行跳，与徽章上的 `[U 上] [D 下]` 正好相反。现已对齐：**D/n = 下一个（往下行循环）、U/N = 上一个（往上行循环）**；回车确认后指针落在**第 1 个**匹配，按 D 顺序往下走、按 U 回绕到最后一个。
- **② 删除「直接鼠标拖动即选区/自动复制」**：终端里普通左键拖动不再起选区、也不再自动复制；选择统一走复制模式（Ctrl+B `[`）里的 Shift/Alt 两角点选，或进入复制模式后用键盘 Shift/Alt 移动。
- **③ 复制模式开始选择改用 Shift/Alt，去掉 Space/v/V/b**：在光标处按 **Shift+方向** 开始/扩展行（流式）选区、**Alt+方向** 开始/扩展矩形（框）选区；不再用 Space/v（开始）和 b（切换块选）。
- **④ Alt 框选宽度恒为 2 的倍数**：矩形选区按「每个字符占 2 列」对齐，宽度为奇数时把活动端再外扩一列——即使光标在第 3 列（奇数格数），也按 4 格框选，保证框边永远落在整字边界、不会切到半个汉字。渲染高亮与复制内容都按此偶数宽度对齐。
- **⑤ 复制模式无修饰（直接方向）移动默认丢弃当前高亮**：不加 Shift/Alt 直接按方向键只移动光标，会清掉已有选区高亮（从头再来）；可用设置页新增的 **copy_move_deselect（复制模式直接移动丢弃高亮）** 关闭，以保留选区。`termux.ini` 同步增加该配置项，热重载。
- **验证**：`verify_copy_mode.py` 增加不变量（Space/v/V/b 已移除、无修饰移动清高亮、块选偶数宽度、配置项存在、普通拖选复制已移除）；`verify_search.py` 更新为「cur 初始=0、D 往下 0→1→2→回0、U 往上」的真实源码断言。单元测试 260 / 0 failed，`verify_all.py` 30 项，gcc / g++ 双 `-Werror`。

### v1.8.27
- **复制模式选择改为标准半开区间（caret）模型，修复「光标像左移一格」和「默认就选中一格」**。此前键盘选区是闭区间：按 Space/v 开始选择时锚点=光标所在格，于是光标所在的那个格子立刻被高亮（默认就选中一格）；端点吸附又让光标视觉上偏移。现按 tmux/vi 复制模式的 caret 语义：
  - **默认不选中任何格子**——锚点 caret 与端点 caret 重合时，高亮范围为空，光标就停在字首的边界上；
  - **→ 一次只选中跨过的那一个字符**：从锚点起按一下 → 越过一个窄字符就恰好高亮那 1 格、越过一个汉字/全角/emoji 就高亮那 2 列，不会多也不会少；← 同理；
  - 光标始终落在字符（宽字符主格）的字首位置，不再出现「光标比选区边界左/右错一格」。
  - 实现：`copy_selection_to_clipboard(...)` 增加 `halfopen` 参数；键盘选区（`!g_copy_quick`）端点为排他 caret，闭合到格子时右端 `-1`；渲染高亮对键盘选区按 `[锚点 caret, 端点 caret)` 计算（空选区不 `sel_active`）。**鼠标拖选与 Shift/Alt 两角点选保持闭合区间 + 整字吸附**（v1.8.26 的行为不变）。
- **验证**：`verify_copy_mode.py` 更新复制函数签名/拖拽包装断言，新增半开区间不变量（`halfopen = g_copy_quick ? 0 : 1`、渲染右端 caret 排他、yank 传半开标志）；`verify_copy_cursor.py` 扩大 yank 检查窗口。单元测试 260 / 0 failed，`verify_all.py` 30 项，gcc / g++ 双 `-Werror`。

### v1.8.26
复制模式选区与搜索的四项改进：
- **① 修复「选区光标向左偏一格」**：v1.8.25 修好 CHAR_INFO 步长后，鼠标端点的「次格→主格」整字化真正生效，但它作用在了选区端点上——鼠标点在一个汉字的右半格（次格）时，端点被向左推到字首，渲染/复制只对「主格」右扩，结果点到的那个汉字反而没被选中。现把**光标位置**（`g_copy_cx`，键盘移动/显示，始终在主格）与**选区端点**（新增 `g_copy_end_x`：键盘=主格光标；鼠标=原始点击列）分离：鼠标点到汉字左半或右半，渲染高亮与复制取色的 `snap_left/right_to_char` 都按方向把该字整字纳入，选区不向左偏、也不切半个字。
- **② 块（矩形）复制「只有需要向右补全的行才补全」**：块选复制不再把每行尾随空格全部裁掉。规则改为：**完全空的行保留为空行**（框选一列「啊 / 空行 / c」可复制出中间带空行的结果）；**有内容但不到块右边界的行，用空格补全到块右边界**（保持矩形列对齐）。流式（行内连续）选区仍按行裁尾随空格。
- **③ 搜索悬停不再重复提示**：搜索徽章本身常驻 `[U 上] [D 下] [×]` 按钮，鼠标悬停时不再向左展开那条重复写着「U 上一个 · D 下一个」的长提示（复制模式徽章的悬停说明保留）。
- **④ 搜索实时预览（VSCode 式）**：在搜索输入框里边打字，就边重算并高亮全部匹配、实时显示 `当前/总数`，视图**不滚动**、指针不钉死；按回车才定位并跳到当前匹配。`execute_search` 拆为 `run_search(live)`（live=1 预览只高亮、live=0 回车定位滚动），插入/退格/删除/清空都会实时刷新，清空关键词时高亮同步消失。
- **验证**：`verify_copy_cursor.py` 加「端点/光标分离、`g_copy_end_x` 记原始列、yank 用端点、普通拖选不强制吸主格」不变量；`verify_copy_mode.py` 更新块复制补全断言；`verify_search.py` 加实时预览的真实源码用例（不滚动、计数=3、cur=0、清空清高亮）与搜索悬停/接线不变量。单元测试 260 / 0 failed，`verify_all.py` 30 项，gcc / g++ 双 `-Werror`。

### v1.8.25
- **修复「复制模式里 → 要按两下才走过一个汉字」的真正根因（CHAR_INFO 步长错位）**。整字吸附/步进/光标函数（`snap_left/right_to_char`、`copy_step_char`、`copy_cursor_to_lead`）此前接收的是 `const WCHAR *line`，取行时用 `&cells[0].Char.UnicodeChar`。但屏幕缓冲里每个单元格是 `CHAR_INFO { WCHAR Char; WORD Attributes; }`，**步长是 4 字节**；把它当 `WCHAR*`（2 字节步长）按列索引时，`line[x]` 读到的是错位后的内容——`line[3]` 甚至读到前一个单元格的属性值 `7`。于是在汉字主格那一列，`copy_step_char` 读到的是别的字符、误判成「窄字符」，**右移只走 1 格落到汉字次格**，必须再按一下才跨过整个汉字。
  - 这些函数的参数统一改为 `const CHAR_INFO *line`，函数内用 `line[i].Char.UnicodeChar`（新增宏 `LCC(line,i)`）按真实步长读字符；
  - 取行处（`copy_line_at_cy`、复制取色 `sline`、渲染 `render_sel_line`）统一返回 `CHAR_INFO*`（`s->lines[pr].cells` / `&s->alt_buffer[...]`），不再取 `&...Char.UnicodeChar`；
  - 修复后在真实缓冲上：RIGHT 从「中」一下跨到「文」再到后续字符，每次跨过一整个汉字；左移、上下移动、鼠标点选/拖选、端点吸附同步恢复正确。
- **测试加固**：三个整字回归脚本（`verify_copy_snap/step/cursor.py`）的测试数组从紧凑 `WCHAR[]`（2 字节步长，恰好掩盖 bug）改为真实布局的 `CHAR_INFO[]`（带 Attributes、4 字节步长），从此能真正按单元格步长验证；`verify_copy_snap.py` 另加源码不变量，禁止再出现 `&cells[0].Char.UnicodeChar` 当整行指针、且要求四个整字函数签名为 `CHAR_INFO*`。
- 单元测试 260 / 0 failed，`verify_all.py` 30 项全过，gcc / g++ 双 `-Werror`。

### v1.8.24
- **修复「当前屏幕上鼠标选中汉字，高亮/光标仍像停在字中间」的真正根因**：v1.8.23 已把光标坐标整字化，但**渲染选区高亮**的端点吸附 `snap_sel_row()` 只有在滚动回历史（`scroll_offset>0`，此时才能算出物理行 `ar>=0`）时才拿到行内容；在**当前屏幕（活屏，`ar=-1`）上直接 return、完全不吸附**。于是在活屏用鼠标拖选中文时，选区高亮把宽字符（中文/全角/emoji）切成半个，视觉上就像光标停在了字中间。
  - 新增 `render_sel_line(s, row, vo)`：渲染时按屏幕行号统一取整行字符——活屏用 `screen_phys_row(s,row)`、回滚历史用 abs→phys 公式、alt 屏用 `alt_buffer`，三种情况都能取到行；
  - `snap_sel_row()` 改为走 `render_sel_line(s, y, vo)`，**活屏/回滚/alt 屏的选区高亮全部整字吸附**，宽字符永远整块高亮、不切半个。
- 配合 v1.8.23 的光标坐标整字化（`copy_cursor_to_lead`），现在从「光标位置」到「选区高亮」到「复制内容」三个环节都按整字处理，彻底没有半个汉字。
- **验证**：`verify_copy_snap.py` 增加源码不变量——`snap_sel_row` 必须经 `render_sel_line(s, row, vo)` 取行（覆盖活屏），旧的按 `ar`（活屏为 -1）取行方式不得残留；`verify_copy_cursor.py` 继续守护光标坐标整字化。单元测试 260 / 0 failed，`verify_all.py` 30 项，gcc / g++ 双 `-Werror`。

### v1.8.23
- **复制模式光标彻底整字化：无论鼠标点选/拖动还是键盘上下左右移动，光标都不会停在汉字中间**。v1.8.22 只让左右键按整字走，鼠标和上下移动仍可停在宽字符的次格（半个汉字）上。本版统一为：**光标永远停在完整字符（宽字符主格）上**。
  - 新增纯函数 `copy_cursor_to_lead()`：光标列若落在某个宽字符的次格（占位 0 / emoji 低代理）上，就整字化退到该字符的主格；落在主格或窄字符上则不动。
  - **鼠标**：Shift/Alt 快速复制的两个角、普通左键拖选的起点与拖动端点，全部经过整字化——鼠标拖到汉字的哪一半，光标都跳到该字的字首，绝不停在半个字上。
  - **键盘**：←/→（h/l）仍一次跨过整个宽字符（`copy_step_char`）；**↑/↓（k/j）换行吗也把光标列整字化**（原来只换行、列可能落在半字上）；`$`/End 行尾、进入复制模式时的初始光标同样整字化。
  - 渲染高亮与复制取色原有的端点吸附（`snap_sel_row` / `snap_left/right_to_char`）保留，与光标整字化形成双保险：光标对整字，选中/复制也是整字，中文/全角/emoji 永不会被切成半个。
- **验证**：新增 `verify_copy_cursor.py`（从真实 `src/screen.c` 抽取 `copy_cursor_to_lead`，断言次格→主格、主格/窄字符不动、边界夹紧；变异「检测到次格却忽略吸附」应被捕获；并断言 input.c 的鼠标两角、普通拖选、上下移动、进入复制模式均已接线）。单元测试 260 / 0 failed，`verify_all.py` 30 项，gcc / g++ 双 `-Werror`。

### v1.8.22
- **修正 v1.8.21 的过冲：复制模式鼠标光标可精确停在文字中间**。v1.8.21 把"整字"规则错误地强加在了**鼠标光标位置**上，导致光标不能停在汉字中间那一列、总被强行推到字首/字尾。正确行为是：
  - **光标位置精确跟随鼠标**——指针点在哪一列光标就在哪一列，包括汉字/全角/emoji 的中间任意一列；
  - **选区高亮与复制内容仍按整字处理**（渲染 `snap_sel_row` 与复制取色 `snap_left/right_to_char` 保留）：光标即使停在半个字上，最终选中的色块和粘出来的文字都不会切成半个汉字；
  - 键盘 ←/→（h/l）仍按一次一个字符移动（跨过整个宽字符）。
  - 即：**光标自由定位，选区/复制按整字**。同时移除了 v1.8.21 为强制光标跳格而加的 `copy_quantize_cursor()`（不再需要）。
- **验证**：`verify_copy_snap.py`（选区端点整字吸附）与 `verify_copy_step.py`（键盘整字步进）仍全过；`verify_copy_mode.py` 的点选语义不变量通过。单元测试 260 checks / 0 failed，`verify_all.py` 29 项，gcc / g++ 双 `-Werror`。

### v1.8.21
- **复制模式里【拖动鼠标】也整字整字地跨过宽字符**：v1.8.20 让键盘 ←/→（h/l）一次跨过整个汉字，但用鼠标拖动扩展选区时，实时的复制光标 `g_copy_cx` 仍会停在汉字的后半格（占位）上，拖过中文是"逐半格"走的。现新增 `copy_quantize_cursor()`，把拖动中的光标按整字对齐：
  - **向右拖**（指针在锚点右侧）：指针落在某个汉字/emoji 的起始格时，终点直接跳到该字的结束格——**一次越过整个汉字**；落在结束格或普通字符上则保持；
  - **向左拖**（指针在锚点左侧）：指针落在汉字的后半格时，终点自动退到该字的起始格。
  - 拖动全程的每一次鼠标移动（MOUSE_MOVED）都经过这个对齐，所以复制光标在中文上移动时永远不停在半个字上，和键盘移动、最终选区、复制内容完全一致。
- **验证**：新增 `verify_copy_quantize.py`——从**真实 `src/screen.c` 抽取 `copy_quantize_cursor()` 与 `cell_is_wide_trail()`**，用中文、连续汉字、emoji 代理对、ASCII、空格、左右两个拖动方向做 15 项断言；变异验证（整字化改成恒返回原坐标）立即 8 处失败。单元测试 260 checks / 0 failed，`verify_all.py` 30 项，gcc / g++ 双 `-Werror` 通过。

### v1.8.20
- **复制模式移动光标一次跨过整个宽字符（中文/全角/emoji）**：v1.8.19 让选区端点在渲染/复制时吸附到完整字符，但复制模式里用键盘 ←/→（h/l）或鼠标拖到汉字中间时，光标仍可能停在汉字的**次格（占位）**上，移动是"一格一格"地走过半个汉字。现新增 `copy_step_char()`：光标永远落在**主格**上——
  - 向右：站在宽字符主格一次跨两列（跳过次格），窄字符/空格走一格；
  - 向左：左边最近的主格（左邻是次格则落到该汉字主格），窄字符走一格；
  - 鼠标拖选的第二角也按方向吸附（锚点在左则终点右扩到完整字、锚点在右则终点左收），快速复制的第一角锚点落在次格时左退到主格。
  - 高亮渲染、复制取色、光标移动三者现在都以"完整字符"为最小单位，复制模式里划过中文/emoji 不会再卡在半个字上。
- **验证**：新增 `verify_copy_step.py`——从**真实 `src/screen.c` 抽取 `copy_step_char()` 及其内部 `cell_is_wide_trail()`**，用含中文、连续汉字、emoji 代理对、ASCII、空格、行首行尾的行做 17 项左右步进断言；变异验证（步进改成每次 1 格）立即 7 处失败。单元测试 260 checks / 0 failed，`verify_all.py` 29 项，gcc / g++ 双 `-Werror` 通过。

### v1.8.19
- **选区端点自动吸附到完整字符，避免选中半个宽字符**：中文/全角/emoji 在终端占两列，鼠标拖选或复制模式移动光标时，选区端点可能正好落在一个宽字符的**中间**——高亮只盖住半个字、复制结果也容易错位。现新增边界吸附：
  - **右端点**落在宽字符主格（中文/全角本格，或 emoji 高代理）→ 自动**右扩一格**，把次格（占位格 / emoji 低代理）整格纳入；
  - **左端点**落在宽字符次格（BMP 占位 `0`，或 emoji 低代理）→ 自动**左退一格**，回到主格；
  - 块选（Alt 拖选）每行两端都吸附；流式选区只在首行吸左、末行吸右，中间整行不变。
  - 高亮渲染与复制取色用同一对吸附函数（`snap_right_to_char` / `snap_left_to_char`），保证**所见即所复制**——选区盖住的范围和粘出来的内容一致，不会出现"高亮半个字、复制却整段"或反过来的错位。
- **验证**：新增 `verify_copy_snap.py`——从**真实 `src/screen.c` 抽取两个吸附函数**，构造含中文、emoji 代理对、全角字符、ASCII、空格、末列越界的行，断言左右端点吸附方向与夹紧行为（16 项）。变异验证：把吸附改成恒返回原坐标立即 6 处失败。单元测试 260 checks / 0 failed，`verify_all.py` 28 项，gcc / g++ 双 `-Werror` 通过。

### v1.8.18
- **修复复制中文 / 全角等宽字符时字间被插入空格**：中文（如"保" U+4FDD）在终端占两列，vt 写入时主格写字、**次格写 0 占位**（`screen_put_cp`）。屏幕渲染遇到占位格直接 `continue` 跳过，但复制路径把 `ch==0` 的占位格一律当空格（`text_ch=(ch!=0)?ch:L' '`），于是每个汉字后多一个空格——`保留所有权利` 复制成 `保 留 所 有 权 利`。占位格还带着同样的属性（含背景色），v1.8.17 的行右边界逻辑还会把它当"彩色空格"保留，属同源问题。
  - 修复：新增 `copy_cell_is_wide_spacer()`（判据：本格 `ch==0` 且左邻是占两列的宽字符；non-BMP emoji 次格存低代理 `ch!=0`，仍由 `cliphtml` 代理对逻辑处理）。复制循环识别到占位格即**跳过**——不进纯文本、不进 HTML，并给对应 `ClipHtmlCell` 打上 `skip` 标记；`cliphtml_frag_row()` 对 `skip` 格整体跳过（不输出空格、不打断同色 run）。真实空格、行内空白、ASCII 后空格均不受影响。
- **验证**：新增 `verify_copy_wide.py`——①从**真实 `src/input.c` 抽取 `copy_cell_is_wide_spacer()`** 做单元断言（中文/全角/平假名占位格跳过；真实空格、ASCII 后空白、emoji 代理、行首不跳过）；②链接**真实 vt/screen/cliphtml** 喂 UTF-8 中文串端到端断言生成 HTML 与纯文本无中间空格（`' 保留所有权利'` → 1 前导空格 + 6 汉字）。变异验证：把判定改成恒返回 0 立即 3 处失败。单元测试 260 checks / 0 failed，`verify_all.py` 27 项，gcc / g++ 双 `-Werror` 编译通过。

### v1.8.17
- **修复复制（HTML Format）时行尾"带背景色的空格"被当行尾空白裁掉**：用户实测粘到 Word 时色块背景仍有缺失。用真实 `colortool -c` 字节日志走完整复制路径（真实 `input.c` 取色 → 真实 `cliphtml.c` 生成 CF_HTML）定位到：colortool 每个色块是 `  gYw  `——**末尾两个空格同样带着背景色**（是底色块的一部分）。但复制 HTML 的行右边界 `valid_x1` 只记"最后一个**非空格** cell"，于是每行最后一个色块（47m）的彩色尾随空格被整段丢弃，最后一列色块的右半底色丢失。
  - 修复：新增 `cliphtml_row_right_boundary()`，HTML 行右边界从"最后非空格"扩展为"最后一个**带颜色**的格"——带背景色或非默认前景色的尾随空格一律保留（看得见的色块），只裁真正透明（无底色）的行尾空白；一旦碰到无颜色的空格就停住（它后面全是默认背景）。纯文本的行尾 trim 逻辑不变（记事本等仍得到干净文本）。
- **验证**：新增 `verify_copy_trailing_bg.py`——从**真实 `src/input.c` 抽取 `cliphtml_row_right_boundary()` 函数体**编译执行，断言带 `bg_valid`/`fg_valid` 的尾随空格保留、透明尾随空格仍裁、空行返回 `x_start-1`、块选边界正确。变异验证：把"带颜色空格保留"改回"遇空格即停"立即 4 处失败。真实 colortool 字节端到端回放：灰底色块 span 由修复前的 `  gYw`（丢尾随空格）变为 `  gYw  `（含两个背景色空格）。单元测试 260 checks / 0 failed，`verify_all.py` 26 项，gcc / g++ 双 `-Werror` 编译通过。

### v1.8.16
- **修复复制（HTML Format）时 16 色红↔蓝、黄↔青对调**：复制保留颜色走两条取色路径——真彩色（`fg_rgb_on/bg_rgb_on`）直出 `#rrggbb`；16 色按 Campbell 调色板给 RGB。但 cell 属性字（`CHAR_INFO.Attributes`）里的颜色 nibble 是 **Windows 控制台颜色位** 序（红=4、蓝=1、绿=2、黄=6、青=3），而 Campbell 调色板表（`cliphtml_palette16`）按 **ANSI/VT 色号** 索引（红=1、蓝=4、绿=2、黄=3、青=6）。复制取色函数 `attr_palette_rgb` 直接拿 Windows nibble 去索引 ANSI 调色板，**漏掉了 `render.c` 往终端写 SGR 时一直使用的 `m[]={0,4,2,6,1,5,3,7}` Windows→ANSI 转换**，于是粘到 Word / 浏览器 / 邮件时，红色文字（`38;5;1`）变成蓝色、蓝色变红、黄青互换，前景和背景都错。v1.8.15 修好屏幕显示与背景位后，这个一直存在于复制路径的调色板错位暴露了出来。
  - 修复：`attr_palette_rgb` 先用同一张 `win_to_ansi[]` 表把 Windows nibble 转成 ANSI 色号（含亮位 `|8`）再查 Campbell 表，前景、背景、暗色亮色全部正确，和终端里看到的颜色一致。
- **验证**：新增 `verify_copy_colors.py`——从**真实 `src/input.c` 抽取 `attr_palette_rgb` 函数**、链接**真实 `src/cliphtml.c`** 编译执行，断言 Windows 红(nibble 4)/蓝(1)/绿(2)/黄(6)/青(3) 及高 4 位背景、亮位 `|8` 都映射到正确 Campbell RGB（红 197,15,31 / 蓝 0,55,218 / 亮蓝 59,120,255 等），并断言红样的 R 通道大于蓝样。变异验证：去掉转换表（旧 bug）立即 11 处失败。单元测试 260 checks / 0 failed，`verify_all.py` 25 项，gcc / g++ 双 `-Werror` 编译通过。

### v1.8.15
- **修复 16/256 色背景属性永远是黑（`colortool -c` 色块背景丢失的真正根因）**：用户实测 `colortool -c` 背景一直不显示。这次拿到真实字节日志（`TERMUX_DUMP`）后定位到，colortool 用的是 **256 色背景 SGR `ESC[48;5;Nm`**，win-termux 的 vt 解析器把它量化进 16 色控制台属性（`bg_color`），再由 `build_attr()` 组装成 Windows 属性字（低 4 位前景、高 4 位背景）。而 `build_attr()` 里背景位的算法写错了：
  ```c
  WORD a = ctab[fg & 15] | ((ctab[bg & 15] >> 4) << 4);   // 旧
  WORD a = ctab[fg & 15] | (ctab[bg & 15] << 4);           // 新
  ```
  `ctab[]` 表里存的是**前景**位标志（`FOREGROUND_*`，值都在 0..0xF）。背景应当把同一颜色**左移 4 位**（红 `FOREGROUND_RED=0x04` → `BACKGROUND_RED=0x40`），旧代码却先**右移 4 再左移**——前景值 `>>4` 恒为 0，于是所有走 16/256 色 attr 的程序内容**背景位永远是 0（黑）**。真彩 `ESC[48;2;r;g;bm` 走 `bg_rgb_on` 分支、不经这里，所以 win-termux 自身 UI（标签栏 / 滚动条 / 选区）和真彩程序都正常，唯独 colortool 这类 256/16 色背景整片丢失——这也是为什么它长期没被发现。前景 16/256 色走低 4 位不受影响，所以色样文字颜色一直对。
  - 顺带修正 256 色**背景**量化缺亮位的小问题：6×6×6 色立方高亮度格（前景那段有 `|=8`，背景那段漏了）现在也会置亮；灰阶背景 232–255 映射为亮白(15)/黑(0)。
- **诊断增强**：`TERMUX_DUMP=1` 现在额外写 `render_delta.log`，记录每帧**实际通过帧差分发给宿主终端的增量字节**（`[delta N / full M]`），与 `render_dump.log`（差分前的完整整帧）对照即可判断颜色是「没生成」还是「被差分吞掉」。本次排查正是靠它 + 真实字节一锤定音。
- **验证**：新增 `verify_attr_bg.py`——在 Linux 下用最小 Win32 桩编译**真实的 `src/screen.c` + `src/vt.c` + `src/utf8.c`**，喂真实 colortool 字节流（`ESC[38;5;15m` 亮白前景 + `ESC[48;5;1/2/4m` 红/绿/蓝背景块），断言背景属性位（`BACKGROUND_RED/GREEN/BLUE`）正确出现、`build_attr` 对 bg=0..15 逐色给出正确高 4 位、亮背景带 INTENSITY 位、前景不受影响。变异验证：把修复改回 `(ctab[bg]>>4)<<4` 立即 19 处失败。单元测试 260 checks / 0 failed，`verify_all.py` 24 项，gcc / g++ 双 `-Werror` 编译通过。

### v1.8.14
- **修复脏区渲染下光标位置错乱（滚动 / 刷新时尤甚）**：帧尾的光标序列（`ESC[r;cH ESC[?25h` 定位+显光标、`ESC[?25l` 隐光标）出现在所有内容行之后，而 v1.8.12 的帧差分（`framediff`）按绝对光标定位（CUP）把整帧切成「每行一段」逐行比对——帧尾那个 CUP 会把光标序列**折进「光标所在行」的 chunk**。当那一行的*内容*没变（其它行正在刷输出、或拖动滚动条时光标隐藏、滚回底部时光标重现）时，整个 chunk 与上一帧相同就**整段不发**，光标定位序列被吞掉，终端光标停在上一个发出的 CUP 位置（实测常落在最右侧的滚动条列），表现为「滚动滚动条 / 快速刷新时光标不在正确位置」。
  - 修复：`render_screen()` 记录光标段起点 `cursor_pos`，帧差分**只对前面的 body**（标签栏 + 各内容行 + 弹层）做行级比对，光标序列**逐帧无条件追加**到增量帧末尾，不再参与「行有没有变」的判断。
- **差分终端回归锁死**：新增 `verify_dirty_cursor.py` —— 编译真实 `src/framediff.c`，配一个最小 VT 终端模拟器，对同一系列帧（colortool 刷色块 / 空闲 / 滚动隐藏光标 / 滚回底部重现 / 光标移动，随机数百帧）跑两条路径：A) 逐帧喂整帧；B) 喂真实帧差分增量。逐格比对屏幕（字符 + 真彩/16 色背景）与光标（位置 + 显隐），任一不一致即失败。同时用两条变异验证回归有效：把光标序列折回 body 一起差分（→ 615 帧光标错位）、去掉行首 SGR 复位让颜色哨兵跨帧携带（→ 221 帧背景格发散）。此外该差分也再次确认 v1.8.13 的行首 SGR 复位确实消除了背景串色（修复形态 0 不一致）。单元测试 260 checks / 0 failed，`verify_all.py` 23 项，gcc / g++ 双 `-Werror` 编译通过。

### v1.8.13
- **修复 `colortool -c` 等流式着色程序背景丢失（v1.8.12 脏区渲染回归）**：脏区渲染只把「与上一帧不同的行」发给终端，但终端实际的 SGR 颜色状态停在**上一帧最后发出的那一行**，并不是整帧顺序里本行的上一行。旧渲染器靠跨行携带颜色状态省 SGR —— 一行的第一个 cell 若颜色和上一行末尾相同就不发颜色序列；整帧重绘时这套假设成立，可一旦某行被单独切片重发，本行首 cell 不重发背景 SGR，终端就沿用了另一行（如色块行）留下的背景，导致 `colortool` 刷出的色块背景在后续行刷新时整片消失 / 串色。
  - 修复：`render_screen()` 终端内容循环里，每一行在行首 `CUP` 定位之后立即发 `\x1b[0m` 复位，并把颜色哨兵（`la_attr / la_fr / la_br / la_fv / la_bv`）重置为「未知」，强制本行第一个非空 cell 重发完整前景 / 背景颜色。这样**任意一行被单独发出都与整帧渲染结果一致**，不再依赖前序行的 SGR 状态。每行只多 4 个字节，换来增量帧的颜色正确性。
- **验证**：扩展 `verify_dirty_render.py` —— ① 新增 framediff 行为回归 `test_sgr_row_scope`（模拟 colortool 场景：色块行设背景不动、下一行默认色内容变化，断言增量切片里变化行段的 CUP 之后紧跟 `\x1b[0m` 自成 SGR 作用域、且未变化色块行的背景 SGR 不泄漏进增量）；② 新增 render.c 源码不变量检查（锁定终端行循环行首 CUP 后必须有 `\x1b[0m` 复位与颜色哨兵重置）。两处均做变异验证：删掉行首 `\x1b[0m`、让变化行段不带复位，都会立刻报错。单元测试 260 checks / 0 failed，`verify_all.py` 22 项，gcc / g++ 双 `-Werror` 编译通过。

### v1.8.12
- **复制时保留颜色（HTML Format 剪贴板）**：拖选 / 复制模式 / 块选复制时，除了原有的 `CF_UNICODETEXT` 纯文本，现在还会同时写入 `HTML Format` 富文本格式。粘到浏览器输入框、Word、邮件、富文本编辑器、即时通讯工具时，终端里的前景色 / 背景色 / 加粗 / 下划线一并保留；粘到记事本等纯文本目标时仍是纯文本，完全兼容。
  - 真彩色 cell（`rgb_valid`）直出 `#rrggbb`；16 色 attr 按 Windows Terminal 默认的 **Campbell 调色板**给出，贴出来的颜色和终端里看到的一致。
  - 相邻同色 cell 自动合并成一个 `<span>`（行程编码），HTML 体积不会随字符数膨胀；`<pre style="white-space:pre-wrap">` 保留空格与行首缩进，等宽字体对齐。
  - HTML Format 头部四个字节偏移量（StartHTML / EndHTML / StartFragment / EndFragment）按规范自动计算并补齐；`&` `<` `>` 转义；UTF-16 代理对合成 4 字节 UTF-8（emoji 不截断）；空 cell 按空格处理。
  - 纯逻辑收在新模块 `src/cliphtml.c`（不依赖 Win32），剪贴板 API 仍在锁外调用，不阻塞读线程。
- **脏区渲染（只发变化行）**：整帧仍照常生成，但输出前由新模块 `src/framediff.c` 把整帧 VT 流按绝对光标定位（`\x1b[r;cH`）切成「每行一段字节」，与上一帧影子逐行 `memcmp`，**没变的行一个字节都不发**。空闲时（光标不闪烁、无输出）重绘只发标题等 `always` 段，实测稳态字节从整帧量级降到接近 0；有输出时只发真正变化的行。OSC 标题等不属于任何一行的序列归 `always` 段每帧照发，窗口尺寸变化 / 切标签页 / invalidate 自动整帧重发。
- **验证**：新增 `verify_html_clipboard.py`（编译真实 `cliphtml.c` + ASAN/UBSan，断言四个偏移量精确指向标记、真彩色与 Campbell 16 色字面量、同色 run 合并成一个 `<span>`、`& < >` 转义、代理对合成 emoji、空 cell 与行尾裁剪）和 `verify_dirty_render.py`（编译真实 `framediff.c`，断言首帧全发、内容不变的帧 0 行输出、只改第 2 行时增量只含第 2 行的 CUP 段、OSC 始终在 always、invalidate / 行数变化后强制整帧、光标显隐随所在行走），并入 `verify_all.py`（现 22 项）。两条回归都做过变异验证（改错偏移、破坏 RLE、强制全脏、漏发变化行都会立刻报错）。单元测试 260 checks / 0 failed，gcc / g++ 双 `-Werror` 编译通过。

### v1.8.11
- **修复 alt 屏改大小丢真彩色（BUG-8）**：`screen_resize()` 迁移备用屏幕（vim / htop / less 等全屏程序用的那块）时，`cells` / `fg_rgb` / `bg_rgb` 三个数组都按列数整块搬，唯独 `alt_rgb_valid` 只搬了每行第 0 列 —— 结果一改窗口大小，除最左一列外整屏真彩色标记被清零，颜色瞬间退化成 16 色。现在四个并行数组一起搬。
- **修复搜索当前项被滚出后乱跳（BUG-9）**：搜索命中项被新输出挤出滚动缓冲时，如果用户正停留的那一条被剔除，光标以前会弹到**最新**的一条（`new_count - 1`），浏览位置从最老一端直接跳到最新一端。剔除总是发生在最老的一端，因此现在落到存活项的 index 0，也就是「原当前项之后最近的一条」，继续按 `n` 就能顺着往下走。
- **并行数组搬运统一收口**：BUG-4 与 BUG-8 都是「四个数组漏搬一个」的同一类错误。新增 `line_free()` / `line_fill_blank()` / `line_alloc()`（任一分配失败整行回滚）/ `line_copy()` / `alt_row_copy()` 五个静态辅助函数，`screen_ensure_line`、`screen_scroll_up` / `screen_scroll_down`、`screen_resize`、`screen_free` 里所有分配、清空、行搬运、释放站点全部改走它们，`cells`、`fg_rgb`、`bg_rgb`、`rgb_valid` 从此不可能再各写各的。
- **验证**：新增 `verify_screen_state.py`（从真源码抽取 `screen_resize` / `screen_scroll_up` 及五个辅助函数，用 ASAN + UBSan 编译执行，断言 alt 屏 16×6 → 24×6 后 96 个 cell 的真彩色标记一个不少、断言当前项被剔除后落到最近的存活项且未被剔除时原样跟随平移），并入 `verify_all.py`（现 20 项）；两条断言都做过变异验证（把修复改回旧写法即报错）。`verify_ringbuf_asan.py` 同步抽取新的辅助函数。单元测试 260 checks / 0 failed，gcc / g++ 双 `-Werror` 编译通过。

### v1.8.10
- **修复「搜索多占一行」**：搜索输入框以前是铺满整条底行的（`ui_bottom_row()` 取窗口最低行），而窗口一共只有 `host_rows + 1` 行、最后一行本来就是终端内容 —— 一按 `Ctrl+B /` 就凭空吃掉一行输出。现在输入框和搜索状态徽章一样，是贴右上角的紧凑小框（前缀 6 列 + 输入 24 列 + 提示 12 列 = 42 列，右对齐），只画第 2 行，终端内容一行不少。`ui_bottom_row()` / `SEARCH_PREFIX_COLS` / `search_input_width()` 全部删除，渲染与光标定位共用新的 `search_box_layout()`。
- **验证**：新增 `verify_search_box.py`（抠出真实 `render_search_box()` 编译执行，断言只写第 2 行、绝不碰最后一行、宽度恒定且右边界贴住窗口右缘、光标落在输入区内，覆盖空串/长关键词/中文/窄窗口 5 组样例），并入 `verify_all.py`（现 19 项）；`verify_alignment.py` 把「不得再出现 ui_bottom_row / 不得铺满整行」写成硬断言。

### v1.8.9
- **每个菜单项可以配置「启动默认颜色」**
  - `ChooserItem` 新增 `color` 字段（0 = 跟随默认蓝色，1-8 = 指定标签色）。用这个菜单项新建标签页时，`create_pane_from_item()` 直接把颜色套上去，不用再手动按 `Ctrl+B t` 轮换。
  - `termux.ini` 的 `[menu]` 段扩展为 `序号 = 名称, 命令, 目录(可选), color=N(可选)`；`color=N` 也可以省略目录直接写（`1 = cmd, cmd.exe, color=3`），老配置一行不改照样读。
  - 图形化设置页的菜单项详情页新增第 4 个字段「启动默认颜色」：Tab 可切到该行，`←/→` 循环选色、数字 `0-8` 直选、鼠标可直接点格子；操作按钮行随之下移到第 17 行。命令面板的 panel 编辑器同样加了这一行（紧凑布局由 10 行扩到 12 行）。
  - 选择条第 0 格是「默认」(宽 6)，其后 8 个色块每格宽 3，格子相连；选中格用满强度标签色并加 `[]` 标记，未选中格用暗色 —— 渲染与鼠标热区共用 `item_color_hit()` 一套几何。
- **验证**：新增 `verify_item_color.py`（抠出真实渲染函数编译执行，逐列核对命中几何、断言整条宽度恒定、断言选中/未选中配色），并入 `verify_all.py`（现 18 项）；`verify_settings_ui.py` 增加 11 条断言覆盖数据结构、ini 读写、详情页布局与键鼠交互；单元测试 260 checks / 0 failed，gcc / g++ 双 `-Werror` 编译通过。

### v1.8.8
- **命令面板序号不再上色**：结果行左侧的 `[1]`～`[9]` 统一用行内普通文字色（选中行亮、未选中行灰），不再按 panel 标签颜色铺色块，也不再用琥珀色，一列序号不会再花花绿绿地抢视线。`palette_bg_for_color()` 随之删除。
- **命令面板滚轮改为滚页面**：滚轮直接滚动结果列表窗口（`palette_scroll`），选中项跟着页面等量平移，在窗口里的**相对位置保持不变**；滚到顶 / 到底滚不动时不做任何事。终端 pane 的滚轮行为维持原样（只滚动滚动历史，备用屏幕不响应）。
- **验证**：`verify_alignment.py` 增加 6 条断言（序号不得再引用 `TAB_COLOR_BG` / 琥珀色、必须用行内文字色、滚轮必须让选中项随页面等量平移、不得再单独移动选中项、备用屏幕不得发送方向键）；单元测试 260 checks / 0 failed，`verify_all.py` 17 项与 gcc / g++ 双 `-Werror` 编译全部通过。

### v1.8.7
- **本次修复 / 增强（8 项）**
  - **快捷键显示统一**：界面上一律显示 `Ctrl+B` 这类可读写法，不再混用 ini 里的 `C-b` / `C+b`。新增 `keymap_prefix_describe()`，设置页键位表、帮助页、关于页（`Ctrl+B x` 原本写死）全部改为从 keymap 实时生成。
  - **复制 / 搜索状态改为右上角折叠徽章**：常驻的长提示行被 `render_status_badge()` 取代。复制模式显示琥珀色 `[复制模式 行选区/块选区/点选区/移动光标]`，搜索显示蓝色 `[搜索 "关键词" 3/12]`（关键词超过 16 列截断加 `…`）。**鼠标悬停在徽章上时**才向左展开完整操作提示，按钮列不会因此漂移。
  - **搜索移到右上角**，与复制模式同一套徽章几何，底行不再被搜索状态占用。
  - **搜索上一个 / 下一个改用 `U` / `D`**（原来的 `n` / `N` 继续可用），并且徽章上的 `[U 上]` `[D 下]` `[×]` 三个按钮可以直接用鼠标点击。
  - **键位设置页可切换「是否使用前缀」**：键位表新增 `[前缀] / [直接]` 一列，选中行按 `P` 或直接点该列即可切换；写回 `termux.ini` 时形如 `copy-mode = F8 noprefix`（`no-prefix` / `direct` 同义）。直接键由新的 `keymap_lookup_direct()` 在前缀判定之前分发，帮助页会把这类动作的前缀段标成「直接」。
  - **搜索支持锁定大小写**：`[general]` 新增 `search_case_sensitive`（默认 `false`），行为设置页第 4 个开关可切换；在搜索激活时切换会立刻重算匹配结果。
  - **修复跨标签页的复制 / 搜索串台**：复制模式与搜索现在记录归属的 pane（`ui_modes_claim/cancel/sync_pane`），`switch_pane()` 切标签页时自动取消，不会再出现两个标签页同时处于复制 / 搜索状态、互相抢按键的故障。
- **验证**：单元测试扩到 260 checks（新增 `test_keymap_noprefix`：前缀可读描述、`noprefix` ini 语法、直接键查表与描述）；`verify_search.py` 增加大小写锁定的 3 组断言；`verify_settings_ui.py` 增加键位页前缀列与行为页第 4 个开关的断言；`verify_all.py` 17 项、MinGW gcc / g++ 双 `-Werror` 编译全部通过。

### v1.8.6
- **复制模式增强**
  - 复制模式里按住 **Shift + 方向键** 是行选（文本流选区），按住 **Alt + 方向键** 是块选（矩形框选）；没有开选区时会自动以当前光标为锚点开一个。键盘上也可以用 `V` 直接开块选、`b` 在行选 / 块选之间切换。
  - **Shift + 鼠标点两点**：直接进入复制模式并把两点之间选中（行选）。**Alt + 鼠标点两点**：同样直接进入，但是矩形框选。按住 Shift/Alt 拖动时终点会实时跟随。这个入口排在普通拖拽选择和鼠标事件转发之前，所以在 vim / htop 这类接管了鼠标的全屏程序里也能用。
  - 这种「点选会话」里的按键语义：`Ctrl+C` 或 `Enter` 复制并关闭；`Esc` 直接退出；**其它任何键都会退出复制模式，并且那个键会照常送给终端**（不会吞掉你顺手敲下的第一个字符）。修饰键本身（Shift / Ctrl / Alt / Win / CapsLock 等）不算「其它键」，不会误退出。
  - 块选复制会对每一行取同一段列，并逐行去掉行尾空白；渲染高亮与真正复制到剪贴板的范围来自同一套判定，不会出现「选中的和复制的不一样」。
  - 状态条现在区分「行选区 / 块选区 / 点选区」，提示也更新为 `Enter/Ctrl+C 复制, Shift/Alt+方向改选区, Esc 退出`。
  - 新增 `verify_copy_mode.py` 守住这些不变量（含处理顺序：修饰键过滤 → Esc/Ctrl+C/Enter → 其它键退出；点选分支必须在拖拽选择与鼠标转发之前）。

### v1.8.5
- **本次修复**
  - 颜色选择器（标签页改颜色）从 30 列收窄到 20 列：面板边框按 `CP_W` 自动生成，不再是写死的横线，右边那一大片空白消失了。
  - 色块改成「迷你标签页」观感：鼠标悬停的色块用满强度配色 + 加粗白字（等同激活标签页），其余色块用暗色底 + 灰字（等同未激活标签页）；每个色块之间留一格面板底色的间隙，看起来是 8 个标签而不是一条色带。
  - 修复退出确认对话框选项行只有 26 列、右边框错位的问题：整行按面板宽度补齐，选项改为 `[ Y 确认 ]` / `[ N/Esc 取消 ]` 两个按钮，居中排布。
  - 退出确认对话框支持鼠标：以前 `handle_mouse()` 在确认态直接 return，按钮完全点不动。现在悬停会整块高亮，左键点击即确认 / 取消；热区与高亮都取自同一个 `confirm_exit_button_geom()`，不会再漂移。
  - 新增 `verify_confirm_exit.py`：编译真实渲染函数把对话框画出来，逐行断言四行都恰好 34 列、边框列一致，并核对按钮的绘制位置与鼠标热区一致。

### v1.8.4
- **本次修复**
  - 非默认主题（one-dark / nord / gruvbox-dark / dracula）整体重新调色：加深背景、提亮正文与次要文字、强化强调色与按钮字色。现在每套主题都必须通过对比度门槛（正文/底色 ≥ 7:1、次要文字 ≥ 3.2:1、按钮深色字 ≥ 4:1 等），由单元测试逐项断言。
  - 修复 `send-prefix` 在设置页与帮助页永远显示为 `Ctrl+B Ctrl+B` 的问题：未绑定时才显示“连按两次前缀”，一旦在 `[keys]` 或设置页给它录制了按键，就显示并使用真实键位。
  - 修复命令面板的“退出 termux”绕过 `confirm_on_exit` 的问题：改为走统一的 `ACT_QUIT` 动作，快捷键与命令面板行为一致。
  - 命令面板（设置面板）新增「外观 / 主题」「键位设置」「行为开关」三个入口，可直接跳进设置页对应分类。
  - 命令面板刚打开时（查询框还是空的），方向键 ↑/↓/←/→、PageUp/PageDown、Ctrl+P/N 以及数字 1-9 会直接把焦点切到结果列表并生效，不必先按 Tab；一旦开始输入过滤词，数字与字符仍然是搜索内容，切换回结果列表用 Tab。底部提示的「数字:」标注会随之在“选当前 / 搜索”之间切换。
  - 修复外观 / 键位 / 行为三页仍然显示（且位置错乱的）文本光标的问题：这三页没有输入框，统一隐藏光标；只有菜单项文本编辑与颜色十六进制编辑时才显示，并且光标会精确落在十六进制输入位置。
- **配置体系升级：键位、主题、行为全可配**
  - `termux.ini` 由单一菜单列表扩展为四个段：`[general]` 行为、`[theme]` 配色、`[keys]` 键位、`[menu]` 新建菜单；老配置（无段落或 `[settings]`/`[menu]`）继续原样读入。
  - **键位表 (`src/keymap.c`)**：新增动作表 + 绑定表，`handle_prefix()` 改为「查表 → `action_execute()`」分发。前缀键（默认 `Ctrl+B`）与全部 17 个动作的按键都能在 `[keys]` 段重绑，写法支持 `C-`/`M-`/`S-` 修饰、`F1`-`F24` 与 `Space`/`Esc`/`PgUp` 等命名键。连按两次前缀键仍然把前缀键本身发给当前 pane，前缀改成 `C-a` 后发送的也随之变为 `0x01`。
  - **帮助页快捷键实时生成**：帮助页不再硬编码 `Ctrl+B x`，而是从 keymap 取当前键位，改了配置立刻同步。
  - **主题引擎 (`src/theme.c`)**：内置 `github-dark`（默认）、`one-dark`、`nord`、`gruvbox-dark`、`dracula`；`[theme]` 段可按 16 个语义角色单独覆盖颜色，界面里 34 个派生色由「角色 + 底色 + 混合比例」自动推导。
  - 主题的实现方式是「渲染输出等长就地重映射」：界面配色一律写成零填充的 `\x1b[048;2;033;038;045m` 形式，pane 内容色仍由 `%d` 输出，因此重映射只命中 UI 自身，终端程序的颜色一个字节都不会被改；默认主题走 identity 快路径，零开销。
  - 命令面板（设置面板）新增「切换配色主题」条目，可轮换并写回 `termux.ini`。
- **新增全局行为开关**
  - `scrollback`：滚动历史行数改为运行时配置（200 - 500000，原先固定 10000），对之后新建的 pane 生效。
  - `mouse`：关掉后不再申请控制台鼠标事件，也不打开 VT 鼠标追踪。
  - `copy_on_select`：拖选松开时是否自动复制到剪贴板。
  - `confirm_on_exit`：退出 termux 前弹出 `Y/N` 二次确认框，避免误按关掉正在跑的会话。
- **图形化设置页新增三个分类**
  - 侧栏从「启动 + 菜单项」扩展为「启动 / 菜单项 / 外观·主题 / 键位 / 行为」，鼠标点击或 `Ctrl+↑/↓` 切换，启动页可用 `F2`/`F3`(`K`)/`F4`(`B`) 直达。
  - **外观页**：内置主题带四色预览块，`Enter`/点击即时应用并写回 ini；下方两列列出 16 个语义色（色块 + 十六进制 + 自定义标记 `*`），`Enter` 就地编辑 6 位十六进制，`R` 复位单项，`Ctrl+R` 清除全部自定义。
  - **键位页**：第一行前缀键，其后是全部动作（动作名 / 中文说明 / 当前键位 / `[改]` `[复位]`）。`Enter` 或点击 `[改]` 进入**按键录制**，直接按下目标组合键即可（纯修饰键无效，`Esc` 取消），录制结果覆盖式重绑并立即落盘；条目多于窗口高度时自动滚动。
  - **行为页**：`mouse` / `copy_on_select` / `confirm_on_exit` 三个开关（`Space`/`Enter`/点击切换）与 `scrollback` 步进调节（`←/→` 或 `[-] [+]`）。
  - 渲染与鼠标命中共用 `settings_*()` 几何函数与 `SETTINGS_*` 列偏移常量，命中测试里不再出现写死的行号。
- **测试**
  - 新增 `tests/stub/` 下的 Win32 头文件替身与 `tests/test_config.c`，主题与键位模块可在 Linux 上原生编译并跑 149 条断言（`make unittest`，CI 同步执行）。
  - 新增 `verify_config_theme.py`：校验界面配色全部使用零填充写法、且与主题参考色板双向一一对应（新加界面颜色忘记登记会直接失败），并驱动上面的 C 单元测试。
  - 新增 `verify_settings_ui.py`：校验设置页三个新分类的布局常量互不压行、命中测试不写死几何、渲染与交互一一对应。

### v1.8.3
- **重构分层命令面板**：
  - 命令面板现在仅由 `Ctrl+B :` 打开（兼容中文全角冒号 `：`），移除 `Ctrl+B P`、`Ctrl+B Space` 与 `Ctrl+Shift+P` 等歧义快捷键。
  - `Ctrl+B :` 根据当前页面直接进入对应的子面板：普通终端进入“操作命令面板”，图形设置页进入“设置命令面板”。
  - 子面板统一使用同一套几何和命中测试，修复 hover 高亮与鼠标点击错位。
  - 操作面板覆盖新建终端（终端选择子面板支持名称优先的短查询搜索）、自定义命令行、标题/颜色、历史搜索、panel 切换（编号/标题/颜色）、复制模式、热重载、关闭当前 panel 和“退出 termux”；新增“打开设置命令面板”入口。
  - 图形化设置入口归入操作命令面板；“关于”也只在操作命令面板中出现，并打开独立 About panel。设置命令面板保留“打开操作命令面板”入口，并覆盖默认启动项、打开 `termux.ini`、独立添加 panel 条目与菜单项设置。
  - 菜单项设置子面板只列出现有 panel 条目：Enter 编辑，Ctrl+↑/↓ 调整位置；无查询时结果焦点下 X 删除，搜索期间位置调整不可用，输入焦点下 U/D 保留为查询字符，有查询时删除使用 Ctrl+X；添加 panel 仍从设置面板的独立入口执行。
  - 命令面板单次最多显示 9 个结果，按当前可见窗口动态使用 1～9 编号；Tab 在输入框/结果框间切换焦点，Esc 返回时恢复父面板选择、滚动和查询上下文；终端输出处于延迟自动换行时，光标保持显示在最后一格。
  - 压缩编辑 panel 条目的标签/输入/操作行布局，清理父面板残留区域；输入框背景连续覆盖空白、末尾字符、滚动箭头和光标可见范围；文本输入模态会屏蔽全局 Ctrl+B 前缀，避免误开新 panel。
  - “退出 termux”只负责退出整个程序；关闭当前 panel 仍只关闭当前 panel。
- **全界面对齐审查**：
  - 统一弹窗的 ANSI 1-based 左边界与 Windows 鼠标 0-based 锚点，复核 chooser、颜色选择器、重命名、自定义命令、预设库、命令面板、设置页表格/按钮/输入框及 hover 命中区域。
  - 修复颜色选择器 hover 首列偏移；8 个色块现在以连续四单元格绘制，并逐格显式封闭色块/面板背景，避免多余间隙、背景泄漏和边界残留；同时修复 CJK 设置表格名称/命令按字节补空格导致操作按钮错位，以及自定义命令框标题/底部提示宽度不一致。
- **修复搜索提示行定位**：
  - 搜索输入框与搜索结果提示统一使用实时控制台窗口的真实底行，避免窗口调整大小后提示停在最低行上方一行。

### v1.8.2
- **新增命令面板（Command Palette）**：
  - 支持快捷键 `Ctrl+B :` / `Ctrl+B P` / `Ctrl+B Space` 或 `Ctrl+Shift+P` 随时呼出命令面板。
  - 支持关键词实时模糊检索全部 17 项内置功能（新建终端、PowerShell、新建菜单、自定义命令、历史搜索、复制模式、设置、预设库、标签切换/染色/重命名、配置重载、关于、帮助、关闭、退出）。
  - 支持方向键漫游、Enter 执行、Esc 退出，以及鼠标悬停高亮与点击直达执行。
- **修复搜索底栏/提示行定位与对齐 Bug**：
  - 修正搜索状态提示与搜索输入框在不同终端行数下的坐标计算，统一锚定至控制台真实底行（`g_mux.total_host_rows`），彻底消除搜索提示坐落在最低行上方一行的错位问题。

### v1.8.1
- **修复搜索高亮与结果行上移 1 行 Bug**：
  - 精确计算 `screen_scroll_up()` 缓冲区丢弃行数 `dropped = (old_hist + count) - s->hist_lines`，仅在发生历史丢弃（`dropped > 0`）时对搜索匹配项执行绝对行号偏移修正，彻底消除未满历史或边界增长时搜索高亮异常上移 1 行的缺陷。
  - 增强 `render_search_box` 底栏行清除序列（`\x1b[0m\x1b[K`），消除底行残余字符。
  - 在 `README.md` 快捷键列表中补齐 `Ctrl+B /`（搜索滚动历史）。

### v1.8.0
- **滚动缓冲按行懒分配架构（Lazy Line Allocation）**：
  - 将 10000 行环形缓冲区从一次性连续大内存申请重构为按行动态懒分配结构（`ScreenLine *lines`）。
  - 新建或空闲面板内存占用从 ~11MB 骤降至 ~350KB（内存节省超 95%），各行按需分配并在环形滚动绕回时自动回收复用。
- **修复设置界面 Hover 与点击判定向右偏移 1 格 Bug**：
  - 全面修正 `render_settings_panel`、`render_settings_presets` 与 `handle_settings_mouse` 中 0-based 鼠标坐标与 1-based ANSI 控制台输出坐标系的映射偏差。
  - 完美修复默认启动项按钮、新建菜单项表格行与操作按钮（[↑]、[↓]、[改]、[删]）、编辑字段输入框、操作按钮及预设选择列表的 Hover 高亮与点击热区错位。
- **ASAN 内存安全测试真实源码动态抽取与变异验证（BUG-4）**：
  - 重构 `verify_ringbuf_asan.py` 与 `verify_search.py`，改为动态解析并抽取 `include/screen.h`、`src/screen.c` 及 `src/input.c` 中的真实实现，彻底根除静态代码硬编码副本与源码脱钩的隐患。
  - 引入变异测试（Mutation Testing）机制，确保测试套件对环形缓冲溢出及越界读写具备 100% 真实拦截能力。
- **并发临界区安全加固（BUG-5）**：
  - 为 `execute_search()` 与 `copy_range_to_clipboard()` 缓冲区遍历操作引入原子临界区保护（`EnterCriticalSection(&g_mux.cs)`），彻底消除后台 `pane_read_thread` 持续输出与前端主线程遍历 `ScreenBuffer` 之间的数据竞争与撕裂读问题。
  - 剪贴板 Win32 API 调用（`OpenClipboard`/`SetClipboardData`）移至锁外执行，杜绝拖慢后台 I/O 线程。
- **搜索高亮渲染二分查找优化（BUG-6）**：
  - 优化 `render_screen()` 渲染管线，利用搜索匹配列表天然按 `abs_y` 单调递增的特性，在渲染各行前通过二分查找定位当前行匹配区间。
  - 将单帧匹配搜索从 O(cells × matches) 降至 O(rows × log(matches) + cols × matches_in_row)，彻底消除高频字符搜索下的高 CPU 占用与帧率卡顿。
- **搜索结果滚动生命周期自适应同步（BUG-7）**：
  - 在 `screen_scroll_up()` 全屏滚动分支中加入搜索匹配项动态偏移计算：当满历史缓冲区丢弃最旧行时，自动调整所有匹配项的 `abs_y -= count`，丢弃越界项并自适应更新聚焦索引，防止新输出产生时搜索高亮发生行漂移。
- **仓库元数据与生态优化**：
  - 为仓库补齐关键 GitHub Topics 标签（`windows`、`tmux`、`conpty`、`terminal-multiplexer`、`terminal`）。

### v1.7.0
- **模块化架构重构（Multi-file Modular Architecture）**：
  - 将单文件源码全面重构为清晰、高内聚、低耦合的多文件模块化 C 架构：
    - `include/common.h`：核心 Windows 头文件、版本号宏、RGB565 颜色定义与全局常量。
    - `include/types.h`：VT 解析器状态机、屏幕环形缓冲区、多路复用器 `MuxState` 与面板数据结构。
    - `include/utf8.h` / `src/utf8.c`：UTF-8 编解码、宽字符判断、Emoji 字素簇识别与文本行编辑。
    - `include/screen.h` / `src/screen.c`：环形缓冲区管理、真彩色属性存储、局部与全屏滚动、光标与属性构建。
    - `include/vt.h` / `src/vt.c`：ANSI / VT100 / xterm 状态机解析、SGR 属性处理、OSC 标题过滤与路由。
    - `include/config.h` / `src/config.c`：`termux.ini` 配置文件读写、快速预设库、设置编辑器状态维护。
    - `include/pane.h` / `src/pane.c`：ConPTY 伪终端进程生命周期管理、I/O 读写线程、UI 特殊面板（设置/关于）管理。
    - `include/render.h` / `src/render.c`：真彩色 TUI 渲染引擎、多标签栏、弹窗菜单、设置界面、选色器、搜索框绘制。
    - `include/input.h` / `src/input.c`：键盘与前缀键分发、鼠标交互与滚动条拖拽、复制模式与剪贴板交互、历史滚动搜索匹配与跳转。
    - `src/main.c`：控制台初始化与模式配置、主事件循环、窗口缩放、Ctrl+C/退出信号守卫与清理。
- **构建系统与跨编译器支持**：
  - 新增 `Makefile`，支持一键 `make`（MinGW GCC）、`make cpp`（G++）与 `make test`。
  - 维持全平台 **0 Warnings, 0 Errors**（MinGW GCC、G++ 及 MSVC 100% 编译通过）。
  - 更新 GitHub Actions CI 构建流程与所有回归测试套件。

### v1.6.2
- **修复快捷键优先级与去除未组合键映射**：
  - 修复 `Ctrl+B /` 因键码重叠被优先误识别为帮助的 Bug，确保 `Ctrl+B /` 准确触发搜索，`Ctrl+B ? / h` 准确触发帮助。
  - 移除了 `Ctrl+B =` 误触发新建菜单的映射，仅保留标准 `Ctrl+B +`。
- **修复搜索底栏文字溢出换行 Bug**：
  - 优化 `render_search_box` 的列宽动态计算与边框留白，严格将整行字符控制在当前终端列宽之内，彻底消除提示文字溢出导致终端产生换行抖动的问题。
- **界面极简净化**：
  - 全量移除终端向上翻滚及帮助页面底部的 `[%d%%]` 浮动百分比徽标，彻底杜绝浮层遮挡终端右上方与底部的文字内容。

### v1.6.1
- **新增 10000 行历史滚动搜索功能（Scrollback History Search）**：
  - 新增快捷键 `Ctrl+B /`（或在复制模式下按 `/`）呼出底栏搜索输入框。
  - 支持不区分大小写的多行/全历史快速关键字检索，毫秒级跨 10000 行环形缓冲区匹配所有结果。
  - 检索结果全屏琥珀色高亮，当前激活匹配橙色聚焦，视口自动跳转定位至匹配行。
  - 支持按 `n` / `N` 顺逆循环跳转下一个/上一个搜索结果，按 `Esc` 随时退出搜索高亮。
- **集成 `verify_search.py` 自动化行为回归测试**：
  - 覆盖关键字跨行匹配、坐标定位、大小写不敏感检索与跳跃导航。

### v1.6.0
- **修复环形缓冲区局部滚动堆溢出 Bug（BUG-1）**：
  - 修复了 `screen_scroll_up()` 局部滚动区分支未使用 `screen_phys_row()` 取模而直接进行裸加法导致的堆越界读写与内存破坏缺陷。
  - 补齐了局部滚动时对 `rgb_valid` 属性数组的完整同步复制，并通过 AddressSanitizer 1200+ 极端环形绕回用例验证（0 溢出，0 泄漏）。
- **修复关闭 UI 标签页时对 NULL 句柄调用 Win32 API（BUG-2）**：
  - 在 `close_pane()` 中为 `hpc`、`pipe_in`、`pipe_out`、`process`、`thread` 等内核句柄增加显式 NULL 守卫，避免对纯 UI 标签（设置、关于）执行无效句柄清理引发异常。
- **修复 `handle_resize` 数据竞争与全局尺寸同步（BUG-3）**：
  - 将全局尺寸更新与屏幕缓冲区尺寸调整统一收敛至临界区原子执行，彻底消除多线程并发重绘时读取到不一致尺寸的竞态隐患。
- **新增原生复制模式与鼠标拖拽框选复制（Copy Mode）**：
  - 新增快捷键 `Ctrl+B [` 进入复制模式：支持方向键 / `hjkl` 自由漫游光标，`Space` / `v` 开启选区，`Enter` / `y` 复制到 Windows 剪贴板并退出，`Esc` / `q` 取消。
  - 增强鼠标框选支持：在终端内容区域直接左键拖拽高亮文字，松开即刻自动复制到剪贴板，智能处理宽字符与行尾空格修剪。
- **新增配置文件热重载（Hot-Reload）**：
  - 新增 `Ctrl+B r` 快捷键，随时热重载 `termux.ini` 配置文件并在界面生效。
- **集成 ASAN 自动化内存安全测试**：
  - 新增 `verify_ringbuf_asan.py` 纳入 `verify_all.py` 与 GitHub Actions CI，全天候守护环形缓冲算术与内存安全。

### v1.5.0
- **修复 MSVC / C++ 模式下编译失败 Bug（P0）**：
  - 修复了 `create_pane_shell_with_dir()` 中 `goto` 跳转跨越带初始化的变量声明（`exp_dir`、`cur_dir`）导致 MSVC `cl` 及 C++ 编译器报错的缺陷。
  - 将变量声明前置至首个跳转前，实现 MinGW GCC（C 模式）、G++ 及 MSVC 100% 双模式零警告零错误构建。
- **根除前缀键与小键盘/功能键冲突 Bug（P1）**：
  - 移除了 `handle_prefix` 中无效且与小键盘冲突的 lowercase 分支（如 `case 'd'` 误触小键盘 4 直接退出、`case 'p'` 误触 F1 切换 pane 等）。
  - 新增对小键盘数字键（`VK_NUMPAD0` ~ `VK_NUMPAD9`）切换 Pane 的原生支持。
  - 清理 `handle_prefix` 中的死代码（`vk == '+'`、`vk == '?'` 等非有效虚拟键码）。
- **版本号收敛至单一宏定义（P2）**：
  - 新增 `#define TERMUX_VERSION "1.5.0"` 单一数据源，关于视图、帮助视图及标头版本号统一宏引用，消除跨文件硬编码与自锁风险。
- **全面引入 GitHub Actions 自动化 CI 构建（P2）**：
  - 新增 `.github/workflows/build.yml` 工作流，覆盖 Ubuntu MinGW（C / C++ 双编译 + `verify_all.py`）与 Windows MSVC 自动化构建验证。
- **全量测试套件精简与行为化重构（P2）**：
  - 彻底清理淘汰所有历史版本字符串断言脚本（`verify_v129` ~ `verify_v150`），避免测试冗余与字符串修改自锁。
  - 保留并集中运行 `verify_picker`（选色器几何/命中/hover）、`verify_flow`（端到端弹窗状态机）、`verify_mouse53`（鼠标优先级/按键回放）、`verify_color8`（调色板粉色渲染回归）及 `verify_emoji`（真实 C 编译 UTF-8/Emoji 解码）5 大核心行为级测试。
- **字符串与内存安全加固（P3）**：
  - 将配置初始化、标题设定等路径中的 `strcpy` 全量替换为带边界截断保护的 `snprintf`。

### v1.4.5
- **修复前缀快捷键 Shift 组合键（`Ctrl+B +` 与 `Ctrl+B ?`）识别失效 Bug**：
  - 将 `handle_prefix` 升级为支持 `(WORD vk, DWORD ctrl, WCHAR uc)` 全量字符识别。
  - 修复了标准键盘下按下 `Ctrl+B` 后再按 `+`（实际产生 `Shift`+`=` / `VK_OEM_PLUS`）和 `?`（实际产生 `Shift`+`/` / `VK_OEM_2`）时因仅匹配未组合键码导致按键失效的缺陷。
  - 统一支持字符级别与虚拟键码双重匹配，无论是主键盘区 `+`/`?`、带 Shift 组合键还是小键盘 `+`/`/` 均可 100% 灵敏稳定触发对应功能。

### v1.4.4
- **修复快速预设库弹窗排版错位**：
  - 修复了 `presets_geom` 几何计算时最小宽度未包含完整长标题（`"┌─ 常用命令行预设 (按数字/回车选择) ┐"`）导致弹窗内容宽度被过度压缩、右边框对齐错位的缺陷，确保弹窗各行像素级精准对齐。
- **修复帮助界面中鼠标说明排版对齐 Bug**：
  - 调整帮助视图（`g_help_lines`）中 `点击 termux` 项的文本间距，确保其右侧说明文字 `打开 / 关闭本帮助` 与其他鼠标操作项严格在同一列（第 21 列）对齐。
- **新增快捷键打开帮助与新建菜单**：
  - 新增 `Ctrl+B ?` / `Ctrl+B h` 快捷键打开/关闭内置帮助视图。
  - 新增 `Ctrl+B +` / `Ctrl+B =` 快捷键直接弹出新建 Pane 菜单（`[+]` Chooser 弹窗，支持选择或输入自定义命令）。

### v1.4.3
- **[P] 快速预设库使用深绿主题色**：
  - 快速预设弹窗标题栏、左侧导航栏 `[P] 快速预设库` 按钮、启动视图底部 `[P] 快速预设` 按钮以及详细配置视图中的 `[从预设库导入]` 按钮全面统一使用 GitHub-Dark 风格深绿主题色（`#1f883d` / `\x1b[48;2;31;136;61m` / `\x1b[38;2;31;136;61m`）。
- **修复首项[↑]与末项[↓]越界点击直接进入修改的 Bug**：
  - 修复了在启动项管理表格中点击第 1 项的 `[↑]` 或最后 1 项的 `[↓]` 时，因条件未命中未提前返回而发生 Fallthrough 错误触发 `load_item_to_editor` 进入编辑模式的缺陷。
  - 针对 `[↑]`、`[↓]`、`[改]`、`[删]` 操作区域点击增加了显式 `return` 拦截，确保非有效移动操作不误触编辑界面。
- **设置标题栏符号替换**：
  - 移除设置面板标题栏的 `⚙️` emoji，替换为与顶栏保持一致的简洁 `*` 标识，提升跨终端等宽字体渲染稳定性。
- **设置面板 100% 全键盘无障碍操作支持**：
  - **启动视图 (Nav 0)**：支持 `←`/`→`/`Space`/`T`/`H` 切换默认启动项；支持 `↑`/`↓` 选择表格项，配合聚焦高亮标记 `▶`；按 `Enter`/`E` 进入编辑；按 `U`/`D`（或 `Shift+↑`/`Shift+↓`）上下移动调序；按 `X`/`Delete`/`Backspace` 快速删除；按 `+`/`A`/`N` 快速新建；按 `P` 呼出预设库；按 `1`..`9` 直接跳转对应项；按 `Esc` 退出设置。
  - **详细配置视图 (Nav 1..N)**：支持 `Tab`/`Shift+Tab` 顺逆切换输入框焦点；支持文本光标编辑（字符输入、左右移、Home/End、退格/删除）；按 `Enter` 快速保存应用；按 `Ctrl+P`/`Alt+P` 打开预设库导入；按 `Ctrl+D`/`Alt+D` 删除当前项；按 `Esc` 返回列表。
  - **预设库弹窗**：支持 `↑`/`↓` 选择预设并按 `Enter` 确认导入，或直接按数字键 `1`..`9` 快捷导入；按 `Esc` 取消。

### v1.4.2
- **设置界面全要素 Hover 高亮效果**：
  - 设置面板（`is_settings`）激活状态下，鼠标移动事件实时响应并驱动界面重绘。
  - 左侧导航栏（`启动 (Startup)`、各菜单项、`[+] 添加新条目`、`[P] 快速预设库`、`[Ctrl+S] 保存配置`）全面支持鼠标 Hover 高亮样式反馈。
  - 启动项管理视图（单选选项、各行操作按钮 `[↑]`、`[↓]`、`[改]`、`[删]`、底部快捷按钮）及详细配置视图（输入框悬停暗亮提示、`[保存并应用此项]`、`[从预设库导入]`、`[删除此项]` 操作按钮）均增加精准的 Hover 视觉高亮。
- **去除设置界面重复保存按钮**：
  - 移除启动视图右侧主区域底部的重复 `[Ctrl+S] 保存配置` 按钮，保留左侧导航栏底部的唯一全局保存按钮与 `Ctrl+S` 快捷键，消除界面冗余。

### v1.4.1
- **锁定关于与设置标签颜色与标题（修复修改Bug）**：
  - 针对独立关于面板（`is_about`）与设置面板（`is_settings`）增加保护机制，严格禁止通过右键标签菜单弹出修改颜色/重命名弹窗。
  - 屏蔽在关于与设置标签页激活时的 `Ctrl+B t` / `Shift+t` 轮换标签颜色快捷键操作，并屏蔽外部程序 OSC 标题修改，确保系统内置常驻标签的标题与标识色恒定。
- **设置主题色换为浅蓝色（Light Blue）**：
  - 将设置标签页主题色、顶栏右侧 `[*]` 按钮高亮色以及设置面板标题栏背景色统一更新为清新浅蓝色调（`#79c0ff` / `\x1b[48;2;121;192;255m`），与 GitHub-Dark UI 深度融合。

### v1.4.0
- **设置面板重构为独立常驻 Panel 标签页**：
  - 点击 `[*]` 或按 `Ctrl+B s` 不再弹出模态浮层，而是直接创建并切换至全功能的 `[设置 ×]` 独立常驻 Panel 标签页（紫色主题，支持多标签自由切换与独立关闭）。
- **左侧分类导航栏与分栏架构（Split View）**：
  - 界面采用专业的分栏设计：左侧为专属导航侧边栏，清晰划分 **`启动 (Startup)`**、分割线 `---`、各已配置的菜单项列表（`cmd`、`PowerShell` 等）、`[+] 添加新条目` 与 `[P] 快速预设库`。
- **启动项与菜单顺序管理视图（点击“启动”进入）**：
  - 支持配置 **默认启动项**（可自由选择启动 Termux 时默认进入终端会话或内置帮助视图）。
  - 支持直观管理 `[+]` 新建菜单的项顺序（提供 `[↑]` 上移、`[↓]` 下移、`[改]` 编辑详情与 `[删]` 移除条目）。
- **菜单项详细配置视图（点击各条目进入）**：
  - 支持单独对每个菜单条目深度配置 **显示名称 (Display Name)**、**启动命令行 (Command Line)** 以及 **启动目录 (Working Directory)**。
  - 启动目录支持留空（默认继承当前工作路径）或输入自定义路径（支持 `%USERPROFILE%` 等环境变量自动展开），并在底层 `CreateProcessW` 启动时直接生效。
  - 提供 `[保存并应用此项]`、`[从预设库导入]` 与 `[删除此项]` 快捷操作。
- **配置文件（termux.ini）持久化升级**：
  - `termux.ini` 升级支持 `[settings]` 配置区段与扩展三元配置项（`序号 = 显示名称, 启动命令行, 启动目录`），完全向下兼容旧版配置。

### v1.3.3
- **拖拽滚动条动态距离渐变与远距隐形**：
  - 解除拖拽状态下对距离的全局锁定，拖拽过程中滚动条透明度实时跟随鼠标横向距离（`dist = 0..10`）进行 10 级平滑渐变衰减。
  - 当鼠标在拖拽中向左平移超过 10 列（`dist > 10`）时，滚动条完全透明隐形，松开或靠近右侧时平滑恢复。

### v1.3.2
- **修复滚动条无法完全透明及高亮后最后一个字符丢失的 Bug**：
  - 修复了渲染循环中有效列宽被错误截断（`text_rc = host_cols - 1`）导致最右侧第 `host_cols` 列在 `dist > 10` 时未被实际终端字符覆盖刷新的缺陷。
  - 确保普通模式每一行渲染时完整输出全部 `host_cols` 个字符（含最右侧字符），当鼠标移开（`dist > 10`）时，滚动条 100% 恢复完全透明，最右侧字符即刻完美重现，彻底消除悬停高亮后末尾字符残留与丢失现象。

### v1.3.1
- **原生全宽与无重排悬浮滚动条架构（彻底解决输入遮挡与空行冲突）**：
  - **原生全宽分配**：ConPTY 与虚拟屏幕默认分配完整终端宽度（`cols = host_cols`），普通模式与 Alt-Screen 模式均享受完整的全宽列排版，彻底避免因列宽收缩导致的行尾输入与输出字符被截断或遮挡。
  - **无重排悬浮滚动条（Overlay Scrollbar）**：普通模式下当鼠标远离滚动条（`dist > 10`）时，滚动条 100% 透明，最右侧第 `host_cols` 列的原生字符直接穿透无损渲染，用户输入文字与终端内容 100% 清晰可见；仅当鼠标靠近右侧（`dist <= 10`）时，滚动条以平滑 RGB 渐变悬浮覆盖。
  - **零重排进出 Alt-Screen**：Alt-Screen 切换仅在虚拟屏幕层完成主屏/副屏无缝交替，彻底移除进出时对 `ResizePseudoConsole` 的调用，根除 ConPTY 触发终端尺寸重排注入 `\r\n` 导致 `nano && echo end` 出现多余空行的缺陷。
  - **全屏程序删除滚动条**：Alt-Screen 模式（nano / vim / less）及独立关于面板直接独占全屏全宽，不渲染任何滚动条。

### v1.3.0
- **直接在 Alt-Screen 渲染与零闪烁启动**：
  - 启动阶段完全移除旧版控制台启动文字与 Splash 文本输出，程序直接切换进入 Alt-Screen（`\x1b[?1049h\x1b[?1003h\x1b[?1006h\x1b[2J\x1b[H\x1b[?25l`）并即刻执行全量 TUI 界面首次渲染。
  - 彻底杜绝嵌套 Termux 运行时启动文本写入父级终端缓冲区的问题，保证宿主终端与嵌套环境界面的极致纯净与秒开体验。
  - 退出时标准发送 `\x1b[?1003l\x1b[?1006l\x1b[?1049l\x1b[?25h\x1b[0m` 指令，完美无残留恢复用户原始控制台屏幕。

### v1.2.9
- **未截断项目悬停预览抑制**：
  - 针对所有带截断预览的 UI 元素（标签栏标题、新建面板项目名称、设置项显示名称、设置项启动命令），增加未截断判断（实际字符列宽 $\le$ 限制阈值）。
  - 仅当文字确实发生溢出截断（包含 `...`）时才触发 1.0s / 1.5s 悬停预览浮层，未截断的项目不再弹出冗余预览，保持界面纯净整洁。
- **滚动条与进度条 ANSI SGR 颜色隔离与防污染**：
  - 重构滚动条滑块（Thumb）、轨道（Track）、进度百分比标签（`[100%]`）及末尾行清除（`\x1b[K`）的 ANSI SGR 颜色渲染管线，全面引入 `\x1b[0;...m` / `\x1b[0m` 属性强制重置，彻底隔绝子终端输出的 ANSI 属性（反显、下划线、加粗等）对父级滚动条与进度条的污染。
- **支持嵌套多层双滚动条独立展示**：
  - 子层与父层 termux 保持统一的 `host_cols - 1` 视口适配，子 termux 在其终端内展示内部滚动条，父 termux 在最外层展示父级滚动条，自然呈现双滚动条层级且互不干扰。

### v1.2.8
- **新建面板弹窗排版紧凑化与名称智能截断悬停**：
  - 新建面板弹窗（`[+]` 菜单）由固定宽重构为基于项目显示名称长度动态计算的紧凑边框（`chooser_geom`），彻底消除右侧大片空白。
  - 选项显示名称超过 15 列时自动截断为前 12 列 + `...` 省略号，悬停 1 秒弹出 `[完整名称] <full_name>` 浮层实时预览。
- **输入框背景与 `<` / `>` 截断渲染修复**：
  - 重构 `render_scrollable_input` 样式继承管线，修复了 `<` 和 `>` 指示符重置背景色导致输入框背景仅渲染 1 格或被截断的问题。
- **表情符号（Emoji）与宽字符输入光标对齐修复**：
  - 修复了输入框光标相对起始位置的 1 列偏移，确保包含复杂 Emoji / CJK 字符时光标精准对齐在文字插入点，杜绝光标提前落入字符上方的错位现象。
- **设置显示名称全部悬停提示与按键即时消除**：
  - 补充设置项显示名称列的 1 秒悬停提示；按任意键即刻清除所有悬停浮层（包括新建菜单浮层与设置浮层）。
- **嵌套运行（Termux 套 Termux）滚动条遮挡与边界隔离**：
  - 强制限制子终端有效列宽（`text_rc <= host_cols - 1`），确保父 termux 滚动条在任何缩放与嵌套模式下始终置顶覆盖并精准隔绝子终端输出。

### v1.2.7
- **输入框仿 nano 水平滚动与 `<` / `>` 越界指示器**：
  - 针对重命名框（Rename）、自定义命令框（Custom Cmd）与设置编辑项对话框（Settings Edit），引入仿 nano 的单行输入平滑横向滚动机制。
  - 当文本长度超出边框时，自动进行内部视口平移，左侧溢出显示 `<`、右侧溢出显示 `>`，光标严格锁定在边框内，彻底消除光标出框破损边框的缺陷。
- **设置显示名称智能截断与独立悬停提示**：
  - 设置面板中项目显示名称超过 10 列时自动截断为前 7 列 + `...` 省略号展示。
  - 鼠标悬停 1 秒弹出 `[完整名称] <full_name>` 浮层实时展示完整显示名称。
- **设置面板列间距紧凑化校准**：
  - 整体将设置主面板宽度调整为紧凑的 54 列，大幅缩减启动命令行与操作按钮 `[↑][↓][改][删]` 之间的留白距离，各列间隔均匀紧凑。
- **悬停浮层按键即时消除响应**：
  - 出现任何悬停提示（Tab 完整标题预览、设置项名称预览、启动命令预览等）时，按任意键（输入命令、快捷键等）立即消除所有悬停浮层，绝不阻碍后续操作。
- **不可滚动时透明度渐变与高对比度增强**：
  - 优化了无历史记录（`hist_lines == 0`）时的色阶对比度与全向鼠标移动监听，确保全高滑块随距离渐变始终清晰可辨。

### v1.2.6
- **常用命令行预设弹窗自适应紧凑宽度**：
  - 快速添加预设面板由固定 50 列重构为基于项目名称与命令长度的**动态自适应宽度计算（`presets_geom`）**，彻底消除右侧大片空白，排版紧凑美观。
- **修复无滚动历史时距离透明度衰减失效 Bug**：
  - 修复在初始状态或不可滚动（`hist_lines == 0`）时鼠标移动未触发动态重绘的条件判断，确保常驻滚动条在任何历史深度下靠近/远离均能 100% 连贯触发 10 级平滑透明度渐变。
- **修复行满载排版时光标显示向前偏移 1 格的边界 Bug**：
  - 修复当单行字符填满最右侧终端列时，延迟自动换行（`wraparound_pending`）状态下光标渲染错误停留在最后一个字符上方（显得向前挪了一个字符）的问题；光标现精准定位至换行就绪位置，与用户真实输入位置完美贴合。

### v1.2.5
- **滚动条常驻与全高度滑块适配**：
  - 滚动条调整为常驻展示状态（除 Alt-Screen 全屏应用外始终可见并支持 10 级距离感应衰减）。
  - 当无历史记录（`hist_lines == 0`，即无法滚动）时，滑块自动扩展为与轨道等高（全高度覆盖），直观传达当前处于不可滚动状态。
- **常用命令行预设名称精简化**：
  - 常用预设库中将 `PowerShell (pwsh)` 直接精简为标准名称 `Pwsh`（启动命令维持 `pwsh.exe`）。

### v1.2.4 (全新关于独立 Panel 面板与设置/预设全面升级)
- **新增独立“关于”信息面板（About Panel）**：
  - 采用橙色（Orange）经典主题色调，在新建面板菜单（`[+]` 弹窗）最底部提供 `[A] 关于 (About)` 入口（支持鼠标点击与键盘 `A`/`a`/`0` 快捷触发）。
  - 点击打开后生成**独立常驻 Panel 标签页**（带专属橙色标签，支持任意标签切换、改名、改色与 `[x]` / `Ctrl+B x` 关闭）。
  - 关于面板详尽呈现：**版本号（v1.2.4）**、**作者（wu_dream813）**、**当前 Windows 真实系统版本号与内部构建号（动态读取）** 以及开源仓库与协议信息，排版严格按中文全角字符精准对齐冒号。
- **设置面板界面排版紧凑化**：
  - 清除 `[↓]` 与 `[改]` 之间、`[改]` 与 `[删]` 之间的冗余空格，操作按钮排列为紧凑美观的 `[↑][↓][改][删]`，并精确同步鼠标悬停与点击命中坐标。
  - 启动命令行列超过 15 列时自动截断为前 12 列 + `...` 省略号显示，鼠标悬停 1 秒弹出浮层实时显示完整启动命令行。
- **常用命令行预设库扩充与优化**：
  - 新增最新版 PowerShell 7 预设：`pwsh.exe`。
  - 将 Git Bash 预设直接简化配置为标准 `bash.exe`。
- **弹窗子框悬停隔离防护**：
  - 当任何子框/弹窗（设置菜单、新建面板菜单、右键菜单、输入框等）处于开启状态时，严格屏蔽弹窗外部区域（标签栏各按钮、滚动条等）的 Hover 悬停反馈，彻底消除弹窗开启时的背景视觉杂讯。

### v1.2.3
- **修复终端输出时滚动视图与滚动条未动态上移锚定的体验 Bug**：
  - 当用户使用滚轮或滚动条向上回滚查看历史记录（`scroll_offset > 0`）时，若子程序持续产生新输出，自动将滚动偏移量按输出行数同步累加（`scroll_offset += count`）。
  - 确保用户当前视口正在阅读的历史行文本保持绝对锚定、绝不随新输出滚动而被迫下移刷新。
  - 滚动条滑块与百分比指示器随着下方新增输出实时自然向上平滑移动，提供完全符合直觉的专业终端视口锁定体验。

### v1.2.2 (彻底解决海量输出卡死与 Ctrl+C 延迟)
- **引入 $O(1)$ 零内存复制环形滚动缓冲架构（Zero-Copy Ring Buffer Engine）**：
  - 重构底层虚拟屏幕行存储机制，采用高阶环形缓冲物理映射算法（`screen_phys_row`），彻底摒弃每次滚屏产生的 `memmove`（由原先每行 15MB 内存拷贝彻底降为 0 字节开销，单行滚屏耗时由微秒级降为纳秒级）。
  - 消除因高频内存复制造成的 CPU 100% 满载与总线阻塞，海量数据流（如 `tree C:\` 几十万行输出）均可全速瞬时吞吐。
  - **Ctrl+C 秒级即时响应**：彻底打通高吞吐数据流下的输入中断链路，按下 `Ctrl+C` 信号即刻直达并终止子进程，无需等待缓冲区漫长排空。
  - **100% 历史全深度保留**：环形物理行解耦彻底消除负数索引风险，`[0%, 100%]` 全深度历史记录任意回滚均丝滑完整。

### v1.2.1
- **彻底修复 90%~100% 深度滚动历史显示空白/丢失的边界 Bug**：
  - 修复在满载滚动历史（10,000 行）或大范围回滚至最顶部（90%~100%）时，历史行绝对索引计算出现负数导致历史文字无法渲染为空白的边界缺陷。
  - 重构 `screen_scroll_up` 与 `screen_scroll_down` 的基准行对齐机制，确保主屏幕行基准 `scroll_top` 始终与总行数严格锁定，保证在 `[0%, 100%]` 全深度范围内任意向上滚动均能完整无损呈现历史字符、属性与真彩色内容。
  - 修复向下回滚时真彩色有效位数组（`rgb_valid` / `alt_rgb_valid`）的同步平移问题。

### v1.2.0 (高吞吐量极速渲染引擎与并发优化)
- **彻底解决海量输出（如 `tree C:\`、大型编译日志等）界面卡顿与阻塞问题**：
  - **分级锁释放架构（Zero-Contention Rendering）**：重构 `render_screen` 渲染管线，仅在内存组装输出流时短暂停留临界区（<0.05ms），将真正的宿主控制台 I/O 输出 `host_write` 移至临界区外部执行，消除控制台 I/O 期间对后台读取线程的排他锁阻塞。
  - **滚动历史缓冲 2500 行均摊批量平移**：重构 `screen_scroll_up` 内存移动逻辑，由原先超出缓冲深度后每行触发全量 `memmove`（15MB/行，导致数万行输出产生上百 GB 无谓内存搬运）优化为一次性均摊平移 2500 行，将内存复制频次降低 2500 倍。
  - **命名管道突发流量大块聚合处理（Burst Batching）**：读取缓冲区扩容至 32KB 并引入 `PeekNamedPipe` 贪婪聚合排空循环，瞬间消化千行并发吞吐，避免 ConPTY 管道满载阻塞子进程。
  - **自适应 60~120 FPS 渲染帧率节流**：在大量连续输出时智能控制宿主重绘帧率，避免每秒上千次无意义控制台重绘造成的 CPU 浪费，交互事件（按键/鼠标/调整大小）依然保持 0 延迟即时响应。

### v1.1.9
- **滚动条 10 级逐格平滑线性距离渐变**：当鼠标光标距离滚动条超过 10 格（或移出 Pane 区域）时，滚动条完全隐藏（输出 `\x1b[0m ` 重置属性并清空），彻底消除远距离视觉干扰；0~10 格内针对每一个横向距离格数（0 至 10 共 11 个独立档位）计算专属平滑 RGB 色阶，实现随鼠标靠近/远离逐格连贯细腻渐显渐隐。
- **滑块与轨道悬停交互优化**：鼠标直接悬停在滑块（按钮）上时，滑块呈现最高亮度白金高光（`\x1b[48;2;225;235;250m`，237 亮度），交互反馈清晰明确；悬停在轨道上或远离时，滑块保持适中亮度，轨道始终保持沉稳暗底细线（`│`），确保悬停滑块明显亮于悬停轨道。

### v1.1.8
- **子终端宽度自动适配滚动条**：子终端工作区宽度自动调整为 `host_cols - 1`，确保右侧滚动条永不遮挡任何终端字符与光标。
- **滚动条独立 Hover 高亮**：当鼠标悬停在滚动条滑块（Thumb）或轨道（Track）上方时，呈现清晰明亮的悬停反馈状态。
- **动态距离感应透明度衰减**：引入基于光标横向距离的动态渐变衰减引擎，当鼠标靠近最右侧时滚动条清晰可见，当鼠标远离滚动条时自动渐变为极低透明度，保持代码与文字界面的极致纯净与专注。

### v1.1.7
- **优化滚动条拖拽与智能中心预测跳转**：当鼠标按中滚动条滑块（Thumb）时保持当前抓取相对位置，仅在上下移动光标时平滑滚动；当点击滚动条轨道空白处时，自动将滑块中心直接平移对齐至鼠标点击位置（智能预测：当滑块长度为偶数且点击在滑块上方时对齐偏上中心，下方时对齐偏下中心）并支持连续拖拽。

### v1.1.6
- **最右侧列新增可视化交互式滚动条（Scrollbar）**：在终端最右侧列实时绘制 GitHub Dark 风格滚动条滑块与轨道，直观展示当前滚动位置与历史深度；支持鼠标在最右侧列直接点击或拖拽滑块快速跳转滚动历史。

### v1.1.5
- **新增标签页标题智能省略与 1.5 秒悬停完整预览**：标签页标题在 Tab 栏中最多占 15 个显示列；超过 15 列时自动截断为前 12 列并平滑添加 `...` 省略号；当用户鼠标悬停在标签页上方达 1.5 秒时，自动弹出浮层实时显示该标签页的完整原始标题（包含 Administrator 身份前缀、完整系统路径等全部信息）。

### v1.1.4
- **彻底修复嵌套 termux 运行时鼠标 Hover 偶发交替闪烁问题**：优化鼠标全向事件跨层边界同步协议，完善父子会话层级悬停状态转移，消除在子 Tab 栏与父 Tab 栏边界处 Hover 交替触发（Hover / 不 Hover 循环闪烁）的现象。

### v1.1.3
- **修复子页面弹窗右侧内容被截断问题**：消除所有弹窗（新建 Pane 菜单、设置主面板、编辑/添加项弹窗、预设弹窗、改名及自定义命令输入框）行首冗余的 `\x1b[K`（行末擦除）指令，确保弹窗作为浮层覆盖时背景终端右侧内容完整保留不被截断与擦除。

### v1.1.2 (Emoji 与字素簇全面优化)
- **彻底解决 Emoji 单次回退删除（Backspace）与向前删除（Delete）**：在终端内部与内置弹窗中全面升级 Unicode UAX #29 字素簇（Grapheme Cluster）引擎，并引入会话级多码元原子回退机制，确保 `cmd.exe` 与 `PowerShell` 下复杂 Emoji（如 `✔️`、`😀`、`👍🏽`、`1️⃣`、`👨‍💻`）只需按 1 次 Backspace 即可完整干净删除。
- **支持光标 $\leftarrow / \rightarrow$ 跨越 Emoji 导航**：在子终端命令行中移动光标时自动感知 Emoji 码元总数，实现整字符跨越，消除光标停留在 Emoji 中间断裂的问题。
- **支持 SMP 全平面 Emoji（如 💯, 😀, 🎉, 💻, 🤖 等）无损渲染与重组**：虚拟屏幕支持高低代理对（Surrogate Pair）存储与还原，彻底解决 SMP 字符渲染为 `??` 或菱形问号 `` 的问题。
- **消除 8-bit C1 控制码误判与吞行故障**：彻底移除 UTF-8 流对过时 8-bit C1 控制字节的误触发，杜绝 Emoji 输出与系统 OSC 提示导致的解析截断和双重滚屏吞行。
- **优化嵌套运行体验**：初始化启用标准 SGR 鼠标全向事件捕获，修复 termux 嵌套运行时子 termux 鼠标 hover 响应及最右侧 `[*]` 边缘渲染。

### v1.1.1 (重大功能发布)
- **新增图形化交互式 TUI 设置面板 (`[*]` / `Ctrl+B s`)**：内置可视化配置编辑器，支持鼠标与键盘全套快捷操作：`Ctrl+↑/↓` 切换选择项、`↑/↓` 直接调整项目排序、`Enter` 编辑条目、`Ctrl+D` 删除条目，右侧操作按钮（`[↑][↓][改][删]`）支持实时鼠标悬停高亮（Hover）。
- **支持快捷预设加载**：预设面板提供 cmd、PowerShell、WSL Ubuntu、Git Bash、Python、Node.js 及自定义命令行等常用开发环境一键填充。
- **新增 `termux.ini` 配置文件持久化**：支持自定义新建 Pane 菜单的条目数量（1~9 项）、显示名称、排列顺序与启动命令行，自动生成模板，设置修改即时同步保存。
- **优化终端字符对齐与图标兼容**：将齿轮图标更换为标准 ASCII `[*]` 符号，重构全角/半角/中文字符列宽排版算法，彻底消除界面错位问题。

### v1.0.5
- 彻底修复 `termux` 嵌套运行（termux 套 termux）时，鼠标移入外层 Tab 栏导致子 termux 标签悬停（Hover）残留卡住的问题。

### v1.0.4
- 更改标题与自定义命令行输入框全面支持 `←` / `→` / `Home` / `End` / `Delete` 任意位置光标导航与编辑。
- 引入字素簇（Grapheme Cluster）算法，支持复杂 Emoji（如 `✍️`、ZWJ连字序列、变体选择符等）一键 Backspace 完整删除。
- 修复光标位置异常停留在状态栏（Tab Bar）末尾的渲染顺序问题。
- 修复自定义命令行输入框右侧边框对齐问题（校准宽度）。
- 统一编译输出与发布资产名为 `termux.exe`，精简 MinGW 编译参数（移除 `-Wextra`）。

### v1.0.3
- 优化新建弹窗切换渲染：自定义命令行输入框完整覆盖旧新建弹窗，彻底消除残余边框与文字。
- 新增进程异常退出保持机制：命令执行报错（退出码非 0）或启动失败时，完整保留错误日志并显示红色提示，按任意键或点击关闭才退出。

### v1.0.2
- 新增在新建 Pane 弹窗中支持 `[3]` 自定义命令行启动（可输入指定命令如 `wsl`、`bash`、`python`、`pwsh` 等运行）。
- 新增终端窗口标题自动同步当前活动子 Pane 标题（支持 OSC 标题及改名实时同步，退出自动恢复原标题）。
- 修复 Windows cmd / ConPTY `title` 命令产生前缀冒号（`:   T`）的解析问题。

### v1.0.1
- 修复终端窗口调整大小（Resize）导致历史滚动记录丢失被清空的问题。
- 修复帮助页面中在标签栏滑动鼠标导致全屏剧烈闪烁的问题（移除 2J 屏幕清除）。
- 修复 Pane 区域鼠标点击与滚轮事件 Y 坐标整体下偏 1 行（未扣除 Tab Bar 高度）的问题。
- 修复 Pane 双击（`DOUBLE_CLICK`）事件在子程序中被误丢弃的问题。
- 解耦 SGR `1006` 与跟踪模式 `1000/1002/1003`，修复 SGR 模式下移动事件泛滥及模式覆盖问题。
- 修复 SGR 模式下按键释放（Button Up）的按键识别，精准区分左/中/右键释放。
- 增加控制台必须使用等宽字体的警告提示。
- 消除 MinGW 编译警告，优化渲染热路径与资源管理。

### v1.0.0 (首个正式发布版)
- 修复右键内容区误弹菜单（移除非标签页右键兜底）。
- 启动提速（移除启动横幅 800ms 人为延迟）。
- 8 号粉色调色板修复（修复 `color & 7` 索引折叠）。
- 鼠标全量诊断日志支持（`TERMUX_DUMP=1`）。
